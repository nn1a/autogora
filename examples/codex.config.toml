# Replace both absolute paths with the installed binary and project database.
[mcp_servers.taskcircuit]
command = "/absolute/path/to/taskcircuit"
args = [
  "serve",
  "--db",
  "/absolute/path/to/project/data/taskcircuit.db",
]
required = true
startup_timeout_sec = 10
tool_timeout_sec = 30
