{
  "name": "hermes-kanban-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "A durable local Kanban MCP server and Claude/Codex worker dispatcher.",
  "type": "module",
  "bin": {
    "kanban-mcp": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/cli.ts",
    "test": "npm run build && tsx --test test/**/*.test.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
