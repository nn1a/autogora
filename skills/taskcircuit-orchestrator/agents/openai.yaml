interface:
  display_name: "TaskCircuit Orchestrator"
  short_description: "Plan and route work through TaskCircuit MCP"
  default_prompt: "Use $taskcircuit-orchestrator to decompose this goal into assigned TaskCircuit tasks and dependencies."
dependencies:
  tools:
    - type: "mcp"
      value: "taskcircuit"
      description: "Durable TaskCircuit planning and routing tools"
      transport: "stdio"
