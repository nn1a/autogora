interface:
  display_name: "Autogora Coordinator"
  short_description: "Plan and route work through Autogora MCP"
  default_prompt: "Use $autogora-coordinator to coordinate this goal through assigned Autogora tasks and dependencies."
dependencies:
  tools:
    - type: "mcp"
      value: "autogora"
      description: "Durable Autogora planning and routing tools"
      transport: "stdio"
