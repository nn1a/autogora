interface:
  display_name: "TaskCircuit Worker"
  short_description: "Execute and report a claimed TaskCircuit task"
  default_prompt: "Use $taskcircuit-worker to execute the assigned task and report its lifecycle through TaskCircuit MCP."
dependencies:
  tools:
    - type: "mcp"
      value: "taskcircuit"
      description: "Durable TaskCircuit task and run lifecycle tools"
      transport: "stdio"
